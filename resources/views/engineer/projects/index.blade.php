@extends('layouts.app') @section('content')<h1 class="mb-6 text-3xl font-bold">公開案件</h1><div class="grid gap-5 md:grid-cols-2">@forelse($projects as $project)<a href="{{ route('engineer.projects.show',$project) }}" class="rounded-xl bg-white p-6 shadow transition hover:-translate-y-1"><h2 class="text-xl font-bold">{{ $project->title }}</h2><p class="mt-3 text-slate-600">{{ Str::limit($project->required_skills,80) }}</p><div class="mt-4 flex flex-wrap gap-2 text-sm"><span class="rounded bg-slate-100 px-2 py-1">{{ $project->process }}</span><span class="rounded bg-slate-100 px-2 py-1">{{ $project->location }}</span><span class="rounded bg-slate-100 px-2 py-1">{{ $project->min_price }}〜{{ $project->max_price??'応相談' }}万円</span></div><p class="mt-4 text-sm text-slate-500">期限 {{ $project->application_deadline->format('Y/m/d') }}</p></a>@empty<p>募集中の案件はありません。</p>@endforelse</div><div class="mt-5">{{ $projects->links() }}</div>@endsection
