@extends('layouts.app') @section('content')<h1 class="mb-6 text-3xl font-bold">送信履歴</h1><div class="space-y-3">@forelse($interests as $interest)<div class="rounded-xl bg-white p-5 shadow"><a class="block" href="{{ route('engineer.projects.show',$interest->project) }}"><strong>{{ $interest->project->title }}</strong><span class="float-right rounded bg-slate-100 px-3 py-1">{{ $interest->status->label() }}</span><p class="mt-2 text-sm text-slate-500">{{ $interest->created_at->format('Y/m/d H:i') }}</p></a>@can('cancel', $interest)<form class="mt-4 border-t pt-4" method="post" action="{{ route('engineer.interests.destroy', $interest) }}" onsubmit="return confirm('「気になる！」を取り消しますか？')">@csrf @method('delete')<button class="rounded bg-red-600 px-4 py-2 text-sm text-white">取り消す</button></form>@endcan</div>@empty<p>送信履歴はありません。</p>@endforelse</div>{{ $interests->links() }}@endsection
