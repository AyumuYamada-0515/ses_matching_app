@extends('layouts.app') @section('content')<div class="mb-6 flex justify-between"><h1 class="text-3xl font-bold">{{ $project->title }}</h1><div><a class="mr-3 text-indigo-600" href="{{ route('sales.projects.edit',$project) }}">編集</a><form class="inline" method="post" action="{{ route('sales.projects.destroy',$project) }}">@csrf @method('delete')<button class="text-red-600" onclick="return confirm('削除しますか？')">削除</button></form></div></div><div class="rounded-xl bg-white p-6 shadow"><p class="whitespace-pre-line">{{ $project->description }}</p><dl class="mt-6 grid gap-4 md:grid-cols-2"><div><dt class="text-slate-500">必須スキル</dt><dd>{{ $project->required_skills }}</dd></div><div><dt class="text-slate-500">工程</dt><dd>{{ $project->process }}</dd></div><div><dt class="text-slate-500">勤務地</dt><dd>{{ $project->location }}</dd></div><div><dt class="text-slate-500">単価</dt><dd>{{ $project->min_price }}〜{{ $project->max_price??'応相談' }}万円</dd></div></dl></div><h2 class="mb-3 mt-8 text-xl font-bold">申請 ({{ $project->interests->count() }})</h2>@foreach($project->interests as $interest)<div class="mb-3 rounded bg-white p-4 shadow">{{ $interest->engineer->name }} — {{ $interest->status->label() }}</div>@endforeach
@endsection
